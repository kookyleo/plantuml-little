// svek::builder - High-level diagram building via Graphviz
// Port of Java PlantUML's svek.GeneralImageBuilder + GraphvizImageBuilder
//
// Orchestrates: collect entities → build DOT → run Graphviz → parse SVG → position entities

// TODO: Full port pending

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {}
}
