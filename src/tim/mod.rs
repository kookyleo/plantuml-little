// tim - TIM expression engine (preprocessor)
// Port of Java PlantUML's net.sourceforge.plantuml.tim package
// Evaluates !function, !procedure, !define expressions.
//
// Note: Our preproc/ module already implements most TIM functionality.
// This module provides the Java-compatible interface structure.

pub mod expression;
pub mod builtin;
