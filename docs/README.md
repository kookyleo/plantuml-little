# Docs

Keep only current, operational documents here.

## Active documents

- `docs/java_rust_diagram_support_and_reference_audit.md`
  - single source of truth for Java authority, coverage, support gaps, and current parity priorities
- `docs/special_ext_reference_split_workflow.md`
  - focused workflow for splitting one noisy failing reference into smaller external probes

## Cleanup rule

- Do not keep stale baseline notes, obsolete migration audits, or roadmap documents that conflict with the current SVG-only parity goal.
- If a document stops matching the active stable authority (`v1.2026.2`) or the current harness, delete or rewrite it instead of leaving it as historical drift.
